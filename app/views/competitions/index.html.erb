<% content_for :title, "Compétitions de Football : Programme TV et Calendrier" %>
<% content_for :meta_description, "Retrouvez le programme TV complet par compétition : Ligue 1, Ligue des Champions, Premier League et plus encore." %>

<div style="padding: 0 10px;">
  <h1 style="font-size: 24px; font-weight: 800; margin: 25px 0 10px; color: #222;">
    Compétitions <span style="color: #007bff;">Football</span>
  </h1>
  <p style="font-size: 15px; color: #666; margin-bottom: 30px;">
    Sélectionnez une ligue pour voir tous les matchs diffusés à la télévision.
  </p>

  <div style="display: grid; grid-template-columns: repeat(2, 1fr); gap: 15px;">
    <% @competitions.each do |comp| %>
      <%= link_to competition_path(comp[:slug]), style: "text-decoration: none; color: inherit;" do %>
        <div style="background: white; border: 1px solid #eee; border-radius: 16px; padding: 25px 15px; text-align: center; transition: transform 0.2s; box-shadow: 0 4px 6px rgba(0,0,0,0.02);">
          <div style="height: 70px; display: flex; align-items: center; justify-content: center; margin-bottom: 15px;">
            <% if comp[:thumbnail].present? %>
              <img src="<%= comp[:thumbnail] %>" alt="Logo <%= comp[:name] %>" style="max-height: 100%; max-width: 100%; object-fit: contain;">
            <% else %>
              <span style="font-size: 40px;">🏆</span>
            <% end %>
          </div>
          <div style="font-weight: 700; font-size: 15px; color: #333;"><%= comp[:name] %></div>
        </div>
      <% end %>
    <% end %>
  </div>
</div>
